@using System.Globalization
@using CriptoVersus.Web.Services
@inject LocalizationService L

<section class="tv-arena-pressure-gauge @StateClass @(IsCompact ? "is-compact" : string.Empty)"
         aria-label="@AriaLabel"
         title="@AriaLabel"
         style="@BuildColorVariables()">
    <div class="tv-arena-pressure-gauge__panel">
        <header class="tv-arena-pressure-gauge__scoreboard">
            <section class="tv-arena-pressure-gauge__score-team tv-arena-pressure-gauge__score-team--left"
                     style="@BuildSideStyle(LeftColor)"
                     title="@string.Format(CultureInfo.CurrentCulture, Tg("pressureTooltip"), DisplaySymbol(LeftSymbol), DisplayText(LeftDisplayName), LeftPressureDisplay)">
                <div class="tv-arena-pressure-gauge__score-identity">
                    <div class="tv-arena-pressure-gauge__score-logo">
                        @if (!string.IsNullOrWhiteSpace(LeftLogoUrl))
                        {
                            <img src="@LeftLogoUrl" alt="@string.Format(CultureInfo.CurrentCulture, Tg("logoAlt"), DisplaySymbol(LeftSymbol))" />
                        }
                        else
                        {
                            <span>@CompactSymbol(LeftSymbol)</span>
                        }
                    </div>

                    <div class="tv-arena-pressure-gauge__score-copy">
                        <span>ASSET A</span>
                        <strong>@DisplaySymbol(LeftSymbol)</strong>
                        <small>@Tg("pressureLabel")</small>
                    </div>
                </div>

                <strong class="tv-arena-pressure-gauge__score-value tv-arena-pressure-gauge__score-value--left">@LeftPressureDisplay%</strong>
            </section>

            <div class="tv-arena-pressure-gauge__score-center">
                <span>@Tg("title")</span>
                <strong>@DisplayStatusLabel</strong>
                <small>LIVE</small>
            </div>

            <section class="tv-arena-pressure-gauge__score-team tv-arena-pressure-gauge__score-team--right"
                     style="@BuildSideStyle(RightColor)"
                     title="@string.Format(CultureInfo.CurrentCulture, Tg("pressureTooltip"), DisplaySymbol(RightSymbol), DisplayText(RightDisplayName), RightPressureDisplay)">
                <strong class="tv-arena-pressure-gauge__score-value tv-arena-pressure-gauge__score-value--right">@RightPressureDisplay%</strong>

                <div class="tv-arena-pressure-gauge__score-identity">
                    <div class="tv-arena-pressure-gauge__score-copy tv-arena-pressure-gauge__score-copy--right">
                        <span>ASSET B</span>
                        <strong>@DisplaySymbol(RightSymbol)</strong>
                        <small>@Tg("pressureLabel")</small>
                    </div>

                    <div class="tv-arena-pressure-gauge__score-logo">
                        @if (!string.IsNullOrWhiteSpace(RightLogoUrl))
                        {
                            <img src="@RightLogoUrl" alt="@string.Format(CultureInfo.CurrentCulture, Tg("logoAlt"), DisplaySymbol(RightSymbol))" />
                        }
                        else
                        {
                            <span>@CompactSymbol(RightSymbol)</span>
                        }
                    </div>
                </div>
            </section>
        </header>

        <div class="tv-arena-pressure-gauge__layout tv-arena-pressure-gauge__layout--score-pattern">
            <section class="tv-arena-pressure-gauge__center">
                <div class="tv-arena-pressure-gauge__gauge-shell">
                    <svg class="tv-arena-pressure-gauge__svg"
                         viewBox="0 0 420 250"
                         role="img"
                         aria-label="@AriaLabel">
                        <defs>
                            <linearGradient id="@($"{_gradientId}-left")" x1="0%" y1="0%" x2="100%" y2="0%">
                                <stop offset="0%" stop-color="@SafeLeftColor" />
                                <stop offset="100%" stop-color="@SafeLeftColor" />
                            </linearGradient>
                            <linearGradient id="@($"{_gradientId}-right")" x1="0%" y1="0%" x2="100%" y2="0%">
                                <stop offset="0%" stop-color="@SafeRightColor" />
                                <stop offset="100%" stop-color="@SafeRightColor" />
                            </linearGradient>
                            <filter id="@($"{_gradientId}-glow-left")" x="-40%" y="-40%" width="180%" height="180%">
                                <feDropShadow dx="0" dy="0" stdDeviation="4" flood-color="@SafeLeftColor" flood-opacity="0.85" />
                            </filter>
                            <filter id="@($"{_gradientId}-glow-right")" x="-40%" y="-40%" width="180%" height="180%">
                                <feDropShadow dx="0" dy="0" stdDeviation="4" flood-color="@SafeRightColor" flood-opacity="0.85" />
                            </filter>
                        </defs>

                        <path d="@ArcPath" class="tv-arena-pressure-gauge__track" pathLength="100" />
                        @foreach (var tick in MajorTicks)
                        {
                            <line x1="@SvgNumber(tick.LineX1)" y1="@SvgNumber(tick.LineY1)" x2="@SvgNumber(tick.LineX2)" y2="@SvgNumber(tick.LineY2)"
                                  class="tv-arena-pressure-gauge__tick-line @(tick.IsCenter ? "is-center" : string.Empty)" />
                        }
                        @foreach (var tick in MinorTicks)
                        {
                            <line x1="@SvgNumber(tick.LineX1)" y1="@SvgNumber(tick.LineY1)" x2="@SvgNumber(tick.LineX2)" y2="@SvgNumber(tick.LineY2)"
                                  class="tv-arena-pressure-gauge__tick-line tv-arena-pressure-gauge__tick-line--minor" />
                        }

                        @if (LeftPressureDisplay > 0)
                        {
                            <path d="@LeftArcSegmentPath"
                                  class="tv-arena-pressure-gauge__fill tv-arena-pressure-gauge__fill--left"
                                  stroke="@SafeLeftColor"
                                  filter="@($"url(#{_gradientId}-glow-left)")">
                                <title>@($"{LeftSymbol}: {LeftPressureDisplay}%")</title>
                            </path>
                        }

                        @if (RightPressureDisplay > 0)
                        {
                            <path d="@RightArcSegmentPath"
                                  class="tv-arena-pressure-gauge__fill tv-arena-pressure-gauge__fill--right"
                                  stroke="@SafeRightColor"
                                  filter="@($"url(#{_gradientId}-glow-right)")">
                                <title>@($"{RightSymbol}: {RightPressureDisplay}%")</title>
                            </path>
                        }

                        <line x1="@SvgNumber(SplitMarker.X)" y1="@SvgNumber(SplitMarker.Y1)" x2="@SvgNumber(SplitMarker.X)" y2="@SvgNumber(SplitMarker.Y2)"
                              class="tv-arena-pressure-gauge__split-marker" />
                    </svg>

                    <div class="tv-arena-pressure-gauge__tick-overlay" aria-hidden="true">
                        @foreach (var tick in MajorTicks)
                        {
                            <span class="tv-arena-pressure-gauge__tick-badge @(tick.IsCenter ? "is-center" : string.Empty)"
                                  style="@BuildTickBadgeStyle(tick)">@tick.Label</span>
                        }
                    </div>

                    <div class="tv-arena-pressure-gauge__duel" aria-hidden="true">
                        <span class="tv-arena-pressure-gauge__arrow tv-arena-pressure-gauge__arrow--left"></span>
                        <span class="tv-arena-pressure-gauge__ball" style="@BallSpinStyle">
                            <img src="/img/bola_da_copa.png" alt="" class="tv-arena-pressure-gauge__ball-image" />
                        </span>
                        <span class="tv-arena-pressure-gauge__arrow tv-arena-pressure-gauge__arrow--right"></span>
                    </div>

                    <div class="tv-arena-pressure-gauge__center-copy">
                        <span>@Tg("currentPressureLabel")</span>
                        <strong>@CurrentPressureDisplay%</strong>
                        <em>@DisplayStatusLabel</em>
                    </div>
                </div>
            </section>

        </div>

        <footer class="tv-arena-pressure-gauge__footer">
            <section class="tv-arena-pressure-gauge__momentum tv-arena-pressure-gauge__momentum--left">
                <span>@Tg("momentumLabel")</span>
                <strong>@DisplaySymbol(LeftSymbol)</strong>
                <div class="tv-arena-pressure-gauge__bars" aria-hidden="true">
                    @foreach (var bar in LeftBars)
                    {
                        <i style="@bar.Style"></i>
                    }
                </div>
                <small>@DisplayText(LeftMomentumLabel)</small>
            </section>

            <section class="tv-arena-pressure-gauge__footer-status">
                <strong>@FooterPrimaryStatus</strong>
                <div class="tv-arena-pressure-gauge__footer-arrows" aria-hidden="true">
                    <span class="tv-arena-pressure-gauge__arrow tv-arena-pressure-gauge__arrow--left"></span>
                    <span class="tv-arena-pressure-gauge__ball tv-arena-pressure-gauge__ball--small">
                        <img src="/img/bola_da_copa.png" alt="" class="tv-arena-pressure-gauge__ball-image" />
                    </span>
                    <span class="tv-arena-pressure-gauge__arrow tv-arena-pressure-gauge__arrow--right"></span>
                </div>
                <small>@FooterSecondaryStatus</small>
            </section>

            <section class="tv-arena-pressure-gauge__momentum tv-arena-pressure-gauge__momentum--right">
                <span>@Tg("momentumLabel")</span>
                <strong>@DisplaySymbol(RightSymbol)</strong>
                <div class="tv-arena-pressure-gauge__bars" aria-hidden="true">
                    @foreach (var bar in RightBars)
                    {
                        <i style="@bar.Style"></i>
                    }
                </div>
                <small>@DisplayText(RightMomentumLabel)</small>
            </section>
        </footer>

        <div class="tv-arena-pressure-gauge__description">
            <span class="tv-arena-pressure-gauge__description-icon">i</span>
            <p>@DisplayDescription</p>
        </div>
    </div>
</section>

@code {
    private readonly string _gradientId = $"tv-pressure-{Guid.NewGuid():N}";

    [Parameter] public string LeftSymbol { get; set; } = string.Empty;
    [Parameter] public string LeftDisplayName { get; set; } = string.Empty;
    [Parameter] public string LeftLogoUrl { get; set; } = string.Empty;
    [Parameter] public double? LeftPressurePercent { get; set; }
    [Parameter] public string LeftMomentumLabel { get; set; } = string.Empty;
    [Parameter] public string LeftColor { get; set; } = "#ff8a1f";
    [Parameter] public string RightSymbol { get; set; } = string.Empty;
    [Parameter] public string RightDisplayName { get; set; } = string.Empty;
    [Parameter] public string RightLogoUrl { get; set; } = string.Empty;
    [Parameter] public double? RightPressurePercent { get; set; }
    [Parameter] public string RightMomentumLabel { get; set; } = string.Empty;
    [Parameter] public string RightColor { get; set; } = "#2ee8f2";
    [Parameter] public double? CurrentPressurePercent { get; set; }
    [Parameter] public string StatusLabel { get; set; } = string.Empty;
    [Parameter] public string Description { get; set; } = string.Empty;
    [Parameter] public bool IsCompact { get; set; }
    [Parameter] public string Culture { get; set; } = AppCultureService.DefaultRouteCulture;

    private const double ArcRadius = 165d;
    private const double ArcCenterX = 210d;
    private const double ArcCenterY = 190d;
    private const string ArcPath = "M45 190 A165 165 0 0 1 375 190";

    private int LeftPressureDisplay => _pressure.Left;
    private int RightPressureDisplay => _pressure.Right;
    private int PressureDelta => LeftPressureDisplay - RightPressureDisplay;
    private int CurrentPressureDisplay => Math.Clamp((int)Math.Round(CurrentPressurePercent ?? Math.Max(LeftPressureDisplay, RightPressureDisplay), MidpointRounding.AwayFromZero), 0, 100);
    private string DisplayStatusLabel => string.IsNullOrWhiteSpace(StatusLabel) ? DefaultStatusLabel : StatusLabel.Trim().ToUpperInvariant();
    private string DisplayDescription => string.IsNullOrWhiteSpace(Description)
        ? Tg("description")
        : Description.Trim();
    private string StateClass => string.Join(" ", BuildStateClasses());
    private string AriaLabel
        => string.Format(CultureInfo.CurrentCulture, Tg("ariaLabel"), DisplaySymbol(LeftSymbol), LeftPressureDisplay, DisplaySymbol(RightSymbol), RightPressureDisplay, DisplayStatusLabel);
    private string BallSpinStyle => BuildBallSpinStyle();
    private string LeftArcSegmentPath => BuildArcSegmentPath(0, LeftPressureDisplay);
    private string RightArcSegmentPath => BuildArcSegmentPath(LeftPressureDisplay, 100);
    private (int Left, int Right) _pressure => NormalizePressures(LeftPressurePercent, RightPressurePercent);
    private string DefaultStatusLabel
        => LeftPressureDisplay switch
        {
            >= 70 => string.Format(CultureInfo.CurrentCulture, Tg("statusCritical"), DisplaySymbol(LeftSymbol)),
            >= 60 => string.Format(CultureInfo.CurrentCulture, Tg("statusControlsArena"), DisplaySymbol(LeftSymbol)),
            _ when RightPressureDisplay >= 70 => string.Format(CultureInfo.CurrentCulture, Tg("statusCritical"), DisplaySymbol(RightSymbol)),
            _ when RightPressureDisplay >= 60 => string.Format(CultureInfo.CurrentCulture, Tg("statusControlsArena"), DisplaySymbol(RightSymbol)),
            _ when Math.Abs(LeftPressureDisplay - RightPressureDisplay) <= 10 => Tg("statusBalanced"),
            _ => Tg("statusTransition")
        };
    private string FooterPrimaryStatus
        => Math.Abs(LeftPressureDisplay - RightPressureDisplay) <= 10
            ? Tg("footerInDispute")
            : LeftPressureDisplay > RightPressureDisplay
                ? string.Format(CultureInfo.CurrentCulture, Tg("footerPressuring"), DisplaySymbol(LeftSymbol))
                : string.Format(CultureInfo.CurrentCulture, Tg("footerPressuring"), DisplaySymbol(RightSymbol));
    private string FooterSecondaryStatus
        => Math.Abs(LeftPressureDisplay - RightPressureDisplay) <= 10
            ? Tg("footerNeutralPossession")
            : LeftPressureDisplay > RightPressureDisplay
                ? Tg("footerLeftDominant")
                : Tg("footerRightDominant");
    private SplitMarkerDto SplitMarker => BuildSplitMarker(LeftPressureDisplay);
    private IReadOnlyList<TickMark> MajorTicks => BuildMajorTicks();
    private IReadOnlyList<TickMark> MinorTicks => BuildMinorTicks();
    private IReadOnlyList<BarStyle> LeftBars => BuildBars(LeftPressureDisplay, true);
    private IReadOnlyList<BarStyle> RightBars => BuildBars(RightPressureDisplay, false);
    private string SafeLeftColor => NormalizeColor(LeftColor, "#ff8a1f");
    private string SafeRightColor => NormalizeColor(RightColor, "#2ee8f2");

    private IEnumerable<string> BuildStateClasses()
    {
        if (LeftPressureDisplay >= 70 || RightPressureDisplay >= 70)
            yield return "critical-pressure";

        if (LeftPressureDisplay >= 60)
            yield return "left-dominant";
        else if (RightPressureDisplay >= 60)
            yield return "right-dominant";
        else if (CurrentPressureDisplay is >= 45 and <= 55)
            yield return "balanced";
        else
            yield return "balanced";
    }

    private static (int Left, int Right) NormalizePressures(double? leftRaw, double? rightRaw)
    {
        var left = leftRaw ?? double.NaN;
        var right = rightRaw ?? double.NaN;

        if (double.IsNaN(left) && double.IsNaN(right))
            return (50, 50);

        if (double.IsNaN(left))
            left = 100d - right;

        if (double.IsNaN(right))
            right = 100d - left;

        left = Math.Clamp(left, 0d, 100d);
        right = Math.Clamp(right, 0d, 100d);
        var total = left + right;
        if (total <= 0.001d)
            return (50, 50);

        var normalizedLeft = (int)Math.Round((left / total) * 100d, MidpointRounding.AwayFromZero);
        normalizedLeft = Math.Clamp(normalizedLeft, 0, 100);
        return (normalizedLeft, 100 - normalizedLeft);
    }

    private static string DisplaySymbol(string? value)
        => string.IsNullOrWhiteSpace(value) ? "--" : value.Trim().ToUpperInvariant();

    private static string DisplayText(string? value)
        => string.IsNullOrWhiteSpace(value) ? "--" : value.Trim();

    private static string SvgNumber(double value)
        => value.ToString(CultureInfo.InvariantCulture);

    private static string CompactSymbol(string? value)
    {
        var normalized = DisplaySymbol(value);
        return normalized.Length <= 4 ? normalized : normalized[..4];
    }

    private string BuildColorVariables()
        => FormattableString.Invariant($"--pressure-left:{SafeLeftColor}; --pressure-right:{SafeRightColor};");

    private string BuildSideStyle(string color)
        => FormattableString.Invariant($"--side-accent:{NormalizeColor(color, "#53c8ff")};");

    private static string NormalizeColor(string? color, string fallback)
        => string.IsNullOrWhiteSpace(color) ? fallback : color.Trim();

    private string Tg(string key, params object?[] args)
        => L.T($"tv.pressureGauge.{key}", Culture, args);

    private static SplitMarkerDto BuildSplitMarker(int leftPercent)
    {
        var point = PointOnArc(leftPercent);
        return new SplitMarkerDto(point.X, point.Y - 16d, point.Y + 16d);
    }

    private static IReadOnlyList<TickMark> BuildMajorTicks()
        => new[] { 0, 25, 50, 75, 100 }.Select(value => BuildTick(value, true)).ToList();

    private static IReadOnlyList<TickMark> BuildMinorTicks()
        => Enumerable.Range(1, 19).Select(index => index * 5).Where(value => value % 25 != 0).Select(value => BuildTick(value, false)).ToList();

    private static TickMark BuildTick(int value, bool major)
    {
        var outer = PointOnArc(value, 0d);
        var inner = PointOnArc(value, major ? 12d : 7d);
        var label = PointOnArc(value, -28d);
        return new TickMark(value, $"{value}%", outer.X, outer.Y, inner.X, inner.Y, label.X, label.Y, value == 50);
    }

    private static string BuildArcSegmentPath(int startPercent, int endPercent)
    {
        var start = Math.Clamp(startPercent, 0, 100);
        var end = Math.Clamp(endPercent, 0, 100);

        if (end <= start)
            return string.Empty;

        var startPoint = PointOnArc(start);
        var endPoint = PointOnArc(end);
        // Cada segmento ocupa no máximo meia circunferência visual do gauge.
        // Mesmo quando a pressão passa de 50%, o trecho continua menor que 180 graus
        // porque o gauge inteiro já é apenas um semicírculo. Usar largeArcFlag=1 aqui
        // faz o SVG escolher o arco longo por baixo e ele acaba sendo cortado/parecendo
        // um pedaço pequeno no canto esquerdo.
        const int largeArcFlag = 0;

        return FormattableString.Invariant(
            $"M{startPoint.X:0.###} {startPoint.Y:0.###} A{ArcRadius:0.###} {ArcRadius:0.###} 0 {largeArcFlag} 1 {endPoint.X:0.###} {endPoint.Y:0.###}");
    }

    private static string BuildTickBadgeStyle(TickMark tick)
        => FormattableString.Invariant($"left:{(tick.LabelX / 420d) * 100d:0.##}%; top:{(tick.LabelY / 250d) * 100d:0.##}%;");

    private string BuildBallSpinStyle()
    {
        var magnitude = Math.Abs(PressureDelta);
        var direction = PressureDelta >= 0 ? 1 : -1;
        var duration = magnitude switch
        {
            >= 30 => 1.45d,
            >= 20 => 1.9d,
            >= 10 => 2.35d,
            >= 5 => 2.85d,
            _ => 3.4d
        };

        var lean = Math.Clamp(magnitude * 0.2d, 0d, 7d) * direction;
        return FormattableString.Invariant($"--ball-spin-duration:{duration:0.##}s; --ball-spin-direction:{direction}; --ball-lean:{lean:0.##}px;");
    }

    private static ArcPoint PointOnArc(int value, double radialOffset = 0d)
    {
        var radius = ArcRadius + radialOffset;
        var angle = Math.PI - ((Math.Clamp(value, 0, 100) / 100d) * Math.PI);
        var x = ArcCenterX + (radius * Math.Cos(angle));
        var y = ArcCenterY - (radius * Math.Sin(angle));
        return new ArcPoint(x, y);
    }

    private static IReadOnlyList<BarStyle> BuildBars(int pressure, bool left)
    {
        var emphasis = pressure / 100d;
        var bars = new List<BarStyle>(20);
        for (var i = 0; i < 20; i++)
        {
            var wave = 0.42d + (Math.Sin((i + 1) * 0.62d + (left ? 0.4d : 1.1d)) * 0.18d);
            var peak = Math.Exp(-Math.Pow(i - (left ? 8.5d : 11.5d), 2d) / 22d) * (0.42d + (emphasis * 0.45d));
            var height = Math.Clamp((wave + peak) * 100d, 18d, 100d);
            bars.Add(new BarStyle(FormattableString.Invariant($"height:{height:0.##}%;")));
        }

        return bars;
    }

    private sealed record ArcPoint(double X, double Y);
    private sealed record TickMark(int Value, string Label, double LineX1, double LineY1, double LineX2, double LineY2, double LabelX, double LabelY, bool IsCenter);
    private sealed record SplitMarkerDto(double X, double Y1, double Y2);
    private sealed record BarStyle(string Style);
}

<style>
    .tv-arena-pressure-gauge {
        --pressure-left: #ff8a1f;
        --pressure-right: #2ee8f2;
        --pressure-track: rgba(206, 232, 255, 0.16);
        --pressure-glass: linear-gradient(180deg, rgba(8, 17, 33, 0.94), rgba(5, 10, 24, 0.92));
        --pressure-border: rgba(98, 145, 191, 0.22);
        position: relative;
        width: 100%;
        max-width: 100%;
        min-width: 0;
        min-height: 400px;
        overflow: hidden;
        height: 100%;
        container-type: inline-size;
        container-name: arena-pressure;
    }

        .tv-arena-pressure-gauge,
        .tv-arena-pressure-gauge *,
        .tv-arena-pressure-gauge *::before,
        .tv-arena-pressure-gauge *::after {
            box-sizing: border-box;
        }

    .tv-arena-pressure-gauge__panel {
        position: relative;
        display: grid;
        grid-template-rows: auto minmax(0, 1fr) auto auto;
        align-content: stretch;
        gap: clamp(0.55rem, 0.9cqi, 0.8rem);
        width: 100%;
        max-width: 100%;
        min-width: 0;
        height: 100%;
        min-height: 400px;
        padding: clamp(0.65rem, 1.05cqi, 0.9rem);
        overflow: hidden;
        border: 1px solid var(--pressure-border);
        border-radius: clamp(20px, 3cqi, 28px);
        color: #f5fbff;
        background: radial-gradient(circle at 50% 0%, rgba(46, 232, 242, 0.08), transparent 38%), radial-gradient(circle at 10% 50%, rgba(255, 138, 31, 0.08), transparent 36%), var(--pressure-glass);
        box-shadow: inset 0 0 0 1px rgba(255,255,255,0.025), 0 24px 54px rgba(0, 0, 0, 0.28), 0 0 38px rgba(13, 33, 56, 0.24);
        isolation: isolate;
    }

        .tv-arena-pressure-gauge__panel::before {
            content: "";
            position: absolute;
            inset: 0;
            z-index: 0;
            border-radius: inherit;
            pointer-events: none;
            background: linear-gradient(90deg, color-mix(in srgb, var(--pressure-left) 10%, transparent), transparent 28%, transparent 72%, color-mix(in srgb, var(--pressure-right) 10%, transparent)), linear-gradient(180deg, rgba(255,255,255,0.04), transparent 14%);
        }

    .tv-arena-pressure-gauge__header,
    .tv-arena-pressure-gauge__layout,
    .tv-arena-pressure-gauge__footer,
    .tv-arena-pressure-gauge__description {
        position: relative;
        z-index: 1;
        min-width: 0;
    }

    .tv-arena-pressure-gauge__scoreboard {
        position: relative;
        z-index: 1;
        display: grid;
        grid-template-columns: minmax(0, 1fr) clamp(112px, 12cqi, 140px) minmax(0, 1fr);
        gap: clamp(0.4rem, 0.85cqi, 0.75rem);
        align-items: center;
        align-content: center;
        min-width: 0;
    }

    .tv-arena-pressure-gauge__score-team,
    .tv-arena-pressure-gauge__score-center {
        position: relative;
        min-width: 0;
        height: clamp(62px, 7.2cqi, 76px);
        min-height: 0;
        max-height: 78px;
        overflow: hidden;
        border: 1px solid rgba(255,255,255,0.08);
        background: linear-gradient(180deg, rgba(4, 13, 26, 0.96), rgba(2, 8, 18, 0.94));
        box-shadow: inset 0 1px 0 rgba(255,255,255,0.04), 0 18px 36px rgba(0,0,0,0.22);
    }

    .tv-arena-pressure-gauge__score-team {
        display: grid;
        grid-template-columns: minmax(0, 1fr) auto;
        gap: clamp(0.35rem, 0.75cqi, 0.62rem);
        align-items: center;
        padding: clamp(0.38rem, 0.78cqi, 0.62rem);
    }

        .tv-arena-pressure-gauge__score-team::before {
            content: "";
            position: absolute;
            inset: 0;
            pointer-events: none;
            opacity: .9;
        }

    .tv-arena-pressure-gauge__score-team--left {
        border-color: color-mix(in srgb, var(--pressure-left) 60%, rgba(255,255,255,.08));
        border-radius: 16px 6px 6px 16px;
    }

        .tv-arena-pressure-gauge__score-team--left::before {
            background: radial-gradient(circle at 6% 50%, color-mix(in srgb, var(--pressure-left) 18%, transparent), transparent 34%), linear-gradient(90deg, color-mix(in srgb, var(--pressure-left) 8%, transparent), transparent 72%);
        }

    .tv-arena-pressure-gauge__score-team--right {
        grid-template-columns: auto minmax(0, 1fr);
        border-color: color-mix(in srgb, var(--pressure-right) 60%, rgba(255,255,255,.08));
        border-radius: 6px 16px 16px 6px;
    }

        .tv-arena-pressure-gauge__score-team--right::before {
            background: radial-gradient(circle at 94% 50%, color-mix(in srgb, var(--pressure-right) 18%, transparent), transparent 34%), linear-gradient(270deg, color-mix(in srgb, var(--pressure-right) 8%, transparent), transparent 72%);
        }

    .tv-arena-pressure-gauge__score-identity {
        position: relative;
        z-index: 1;
        display: flex;
        align-items: center;
        gap: clamp(0.45rem, 1cqi, 0.7rem);
        min-width: 0;
    }

    .tv-arena-pressure-gauge__score-team--right .tv-arena-pressure-gauge__score-identity {
        justify-content: flex-end;
    }

    .tv-arena-pressure-gauge__score-logo {
        display: grid;
        place-items: center;
        flex: 0 0 auto;
        width: clamp(32px, 3.6cqi, 44px);
        aspect-ratio: 1;
        padding: clamp(0.2rem, 0.45cqi, 0.32rem);
        overflow: hidden;
        border: 1px solid rgba(255,255,255,0.1);
        border-radius: 50%;
        background: radial-gradient(circle at 35% 30%, rgba(255,255,255,.16), rgba(2,8,18,.88) 62%);
    }

    .tv-arena-pressure-gauge__score-team--left .tv-arena-pressure-gauge__score-logo {
        box-shadow: 0 0 18px color-mix(in srgb, var(--pressure-left) 32%, transparent), inset 0 0 0 1px color-mix(in srgb, var(--pressure-left) 20%, transparent);
    }

    .tv-arena-pressure-gauge__score-team--right .tv-arena-pressure-gauge__score-logo {
        box-shadow: 0 0 18px color-mix(in srgb, var(--pressure-right) 32%, transparent), inset 0 0 0 1px color-mix(in srgb, var(--pressure-right) 20%, transparent);
    }

    .tv-arena-pressure-gauge__score-logo img {
        display: block;
        width: 100%;
        height: auto;
        object-fit: contain;
        filter: drop-shadow(0 0 12px rgba(255,255,255,.18));
    }

    .tv-arena-pressure-gauge__score-logo span {
        color: #f6fbff;
        font-size: clamp(.72rem, 1.4cqi, 1rem);
        font-weight: 900;
        letter-spacing: .08em;
    }

    .tv-arena-pressure-gauge__score-copy {
        display: grid;
        gap: 0.05rem;
        min-width: 0;
    }

    .tv-arena-pressure-gauge__score-copy--right {
        text-align: right;
    }

    .tv-arena-pressure-gauge__score-copy span,
    .tv-arena-pressure-gauge__score-copy small,
    .tv-arena-pressure-gauge__score-center span,
    .tv-arena-pressure-gauge__score-center small {
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
        text-transform: uppercase;
    }

    .tv-arena-pressure-gauge__score-copy span {
        font-size: clamp(0.48rem, 0.72cqi, 0.58rem);
        font-weight: 900;
        letter-spacing: 0.12em;
    }

    .tv-arena-pressure-gauge__score-team--left .tv-arena-pressure-gauge__score-copy span {
        color: var(--pressure-left);
    }

    .tv-arena-pressure-gauge__score-team--right .tv-arena-pressure-gauge__score-copy span {
        color: var(--pressure-right);
    }

    .tv-arena-pressure-gauge__score-copy strong {
        min-width: 0;
        overflow: hidden;
        color: #f7fbff;
        font-size: clamp(0.86rem, 1.35cqi, 1.08rem);
        font-weight: 900;
        letter-spacing: .04em;
        line-height: 1;
        text-overflow: ellipsis;
        white-space: nowrap;
    }

    .tv-arena-pressure-gauge__score-copy small {
        color: rgba(201,221,243,.68);
        font-size: clamp(0.46rem, 0.68cqi, 0.54rem);
        font-weight: 900;
        letter-spacing: 0.1em;
    }

    .tv-arena-pressure-gauge__score-value {
        position: relative;
        z-index: 1;
        align-self: center;
        font-size: clamp(2rem, 5.1cqi, 3.35rem);
        font-weight: 950;
        letter-spacing: -0.08em;
        line-height: 0.92;
        white-space: nowrap;
    }

    .tv-arena-pressure-gauge__score-value--left {
        color: var(--pressure-left);
        text-shadow: 0 0 22px color-mix(in srgb, var(--pressure-left) 38%, transparent);
    }

    .tv-arena-pressure-gauge__score-value--right {
        color: var(--pressure-right);
        text-shadow: 0 0 22px color-mix(in srgb, var(--pressure-right) 38%, transparent);
    }

    .tv-arena-pressure-gauge__score-center {
        display: grid;
        place-items: center;
        align-content: center;
        gap: 0.08rem;
        padding: clamp(0.35rem, 0.7cqi, 0.55rem);
        border-color: color-mix(in srgb, var(--pressure-right) 38%, rgba(255,255,255,.08));
        border-radius: 999px;
        text-align: center;
        background: radial-gradient(circle at 50% 20%, rgba(46,232,242,.12), transparent 54%), linear-gradient(180deg, rgba(6,20,38,.96), rgba(3,11,23,.96));
    }

        .tv-arena-pressure-gauge__score-center span {
            color: #f5fbff;
            font-size: clamp(0.46rem, 0.72cqi, 0.58rem);
            font-weight: 950;
            letter-spacing: 0.12em;
        }

        .tv-arena-pressure-gauge__score-center strong {
            max-width: 11ch;
            overflow: hidden;
            color: rgba(226,238,251,.86);
            font-size: clamp(0.48rem, 0.76cqi, 0.6rem);
            font-weight: 950;
            letter-spacing: .08em;
            text-overflow: ellipsis;
            text-transform: uppercase;
            white-space: nowrap;
        }

        .tv-arena-pressure-gauge__score-center small {
            color: rgba(226,238,251,.78);
            font-size: clamp(0.48rem, 0.72cqi, 0.58rem);
            font-weight: 950;
            letter-spacing: 0.14em;
        }

    .tv-arena-pressure-gauge__layout {
        display: grid;
        grid-template-columns: minmax(0, 1fr);
        gap: clamp(0.55rem, 1.2cqi, 1rem);
        align-items: center;
        justify-items: center;
        min-height: 0;
        height: 100%;
        padding-block: clamp(0.15rem, .65cqi, .65rem);
    }

    .tv-arena-pressure-gauge__layout--score-pattern .tv-arena-pressure-gauge__center {
        width: min(100%, 920px);
        max-width: 920px;
        height: 100%;
        display: grid;
        place-items: center;
        margin-inline: auto;
    }

    .tv-arena-pressure-gauge__header {
        text-align: center;
    }

        .tv-arena-pressure-gauge__header h3 {
            margin: 0;
            color: var(--pressure-right);
            font-size: clamp(1.05rem, 3.1cqi, 1.8rem);
            font-weight: 900;
            letter-spacing: clamp(0.14em, 0.95cqi, 0.24em);
            line-height: 1.05;
            text-transform: uppercase;
            text-shadow: 0 0 18px rgba(70, 245, 255, 0.34);
        }

        .tv-arena-pressure-gauge__header p {
            margin: 0.28rem 0 0;
            color: rgba(214, 231, 255, 0.68);
            font-size: clamp(0.62rem, 1.5cqi, 0.88rem);
            font-weight: 700;
            letter-spacing: clamp(0.08em, 0.6cqi, 0.14em);
            line-height: 1.25;
            text-transform: uppercase;
        }

    .tv-arena-pressure-gauge__side {
        position: relative;
        z-index: 3;
        display: grid;
        align-content: center;
        gap: clamp(0.45rem, 1.2cqi, 0.8rem);
        min-width: 0;
    }

    .tv-arena-pressure-gauge__side--left {
        justify-items: start;
        text-align: left;
    }

    .tv-arena-pressure-gauge__side--right {
        justify-items: end;
        text-align: right;
    }

    .tv-arena-pressure-gauge__identity {
        display: flex;
        align-items: center;
        gap: clamp(0.45rem, 1.2cqi, 0.8rem);
        min-width: 0;
        max-width: 100%;
    }

    .tv-arena-pressure-gauge__side--right .tv-arena-pressure-gauge__identity {
        flex-direction: row-reverse;
    }

    .tv-arena-pressure-gauge__logo-ring {
        display: grid;
        place-items: center;
        flex: 0 0 auto;
        width: clamp(54px, 7.2cqi, 82px);
        aspect-ratio: 1;
        padding: clamp(0.45rem, 1.15cqi, 0.7rem);
        overflow: hidden;
        border: 1px solid rgba(255,255,255,0.08);
        border-radius: 50%;
        background: radial-gradient(circle at 50% 35%, rgba(255,255,255,0.08), rgba(16, 24, 41, 0.86));
        box-shadow: inset 0 0 0 1px rgba(255,255,255,0.03);
    }

    .tv-arena-pressure-gauge__side--left .tv-arena-pressure-gauge__logo-ring {
        box-shadow: 0 0 24px color-mix(in srgb, var(--side-accent, var(--pressure-left)) 28%, transparent), inset 0 0 0 1px rgba(255,255,255,0.03);
    }

    .tv-arena-pressure-gauge__side--right .tv-arena-pressure-gauge__logo-ring {
        box-shadow: 0 0 24px color-mix(in srgb, var(--side-accent, var(--pressure-right)) 26%, transparent), inset 0 0 0 1px rgba(255,255,255,0.03);
    }

    .tv-arena-pressure-gauge__logo-ring img {
        display: block;
        width: 100%;
        height: auto;
        object-fit: contain;
        filter: drop-shadow(0 0 16px rgba(255,255,255,0.18));
    }

    .tv-arena-pressure-gauge__logo-ring span {
        color: #f6fbff;
        font-size: clamp(0.9rem, 1.8cqi, 1.25rem);
        font-weight: 900;
        letter-spacing: 0.08em;
    }

    .tv-arena-pressure-gauge__copy {
        display: grid;
        gap: 0.22rem;
        min-width: 0;
        max-width: 100%;
    }

        .tv-arena-pressure-gauge__copy strong,
        .tv-arena-pressure-gauge__copy span,
        .tv-arena-pressure-gauge__metric span,
        .tv-arena-pressure-gauge__momentum span,
        .tv-arena-pressure-gauge__momentum small,
        .tv-arena-pressure-gauge__footer-status small,
        .tv-arena-pressure-gauge__center-copy span,
        .tv-arena-pressure-gauge__center-copy em {
            text-transform: uppercase;
        }

        .tv-arena-pressure-gauge__copy strong {
            max-width: 100%;
            overflow-wrap: anywhere;
            color: #f7fbff;
            font-size: clamp(0.95rem, 2.25cqi, 1.7rem);
            font-weight: 900;
            letter-spacing: 0.04em;
            line-height: 1;
        }

        .tv-arena-pressure-gauge__copy span {
            max-width: 100%;
            overflow-wrap: anywhere;
            color: rgba(210, 226, 245, 0.72);
            font-size: clamp(0.62rem, 1.45cqi, 0.88rem);
            font-weight: 700;
            letter-spacing: 0.08em;
            line-height: 1.25;
        }

    .tv-arena-pressure-gauge__side--left .tv-arena-pressure-gauge__copy strong,
    .tv-arena-pressure-gauge__side--left .tv-arena-pressure-gauge__metric strong,
    .tv-arena-pressure-gauge__side--left .tv-arena-pressure-gauge__momentum strong {
        color: color-mix(in srgb, var(--side-accent, var(--pressure-left)) 92%, white 8%);
        text-shadow: 0 0 18px color-mix(in srgb, var(--side-accent, var(--pressure-left)) 24%, transparent);
    }

    .tv-arena-pressure-gauge__side--right .tv-arena-pressure-gauge__copy strong,
    .tv-arena-pressure-gauge__side--right .tv-arena-pressure-gauge__metric strong,
    .tv-arena-pressure-gauge__side--right .tv-arena-pressure-gauge__momentum strong {
        color: color-mix(in srgb, var(--side-accent, var(--pressure-right)) 92%, white 8%);
        text-shadow: 0 0 18px color-mix(in srgb, var(--side-accent, var(--pressure-right)) 24%, transparent);
    }

    .tv-arena-pressure-gauge__metric {
        display: grid;
        gap: 0.18rem;
    }

        .tv-arena-pressure-gauge__metric strong {
            font-size: clamp(1.75rem, 5.2cqi, 3.15rem);
            line-height: 1;
            font-weight: 900;
            letter-spacing: -0.05em;
        }

        .tv-arena-pressure-gauge__metric span {
            color: rgba(217, 232, 246, 0.62);
            font-size: clamp(0.64rem, 1.45cqi, 0.9rem);
            font-weight: 800;
            letter-spacing: 0.12em;
        }

    .tv-arena-pressure-gauge__center {
        position: relative;
        z-index: 1;
        min-width: 0;
        overflow: hidden;
    }

    .tv-arena-pressure-gauge__gauge-shell,
    .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__gauge-shell {
        position: relative;
        width: min(100%, clamp(520px, 64cqi, 720px));
        max-width: 720px;
        min-width: 0;
        min-height: 0;
        margin-inline: auto;
        aspect-ratio: 420 / 280;
        overflow: hidden;
        isolation: isolate;
        align-self: center;
    }

    .tv-arena-pressure-gauge__svg {
        position: absolute;
        inset: 0 0 auto;
        z-index: 1;
        display: block;
        width: 100%;
        height: auto;
        overflow: hidden;
    }

    .tv-arena-pressure-gauge__track,
    .tv-arena-pressure-gauge__fill,
    .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__track,
    .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__fill {
        fill: none;
        stroke-width: 34;
        stroke-linecap: butt;
        vector-effect: non-scaling-stroke;
    }

    .tv-arena-pressure-gauge__track {
        stroke: rgba(126, 155, 188, 0.18);
    }

    .tv-arena-pressure-gauge__fill {
        opacity: 1;
        transition: d 420ms ease, opacity 260ms ease, filter 260ms ease;
    }

    .tv-arena-pressure-gauge__fill--left {
        stroke: var(--pressure-left);
    }

    .tv-arena-pressure-gauge__fill--right {
        stroke: var(--pressure-right);
    }

    .tv-arena-pressure-gauge.left-dominant .tv-arena-pressure-gauge__track {
        stroke: color-mix(in srgb, var(--pressure-left) 12%, rgba(126,155,188,0.18));
    }

    .tv-arena-pressure-gauge.right-dominant .tv-arena-pressure-gauge__track {
        stroke: color-mix(in srgb, var(--pressure-right) 12%, rgba(126,155,188,0.18));
    }

    .tv-arena-pressure-gauge__tick-overlay {
        position: absolute;
        inset: 0;
        z-index: 2;
        pointer-events: none;
    }

    .tv-arena-pressure-gauge__tick-line {
        stroke: rgba(203, 228, 255, 0.32);
        stroke-width: 3.2;
        stroke-linecap: round;
        opacity: .9;
    }

        .tv-arena-pressure-gauge__tick-line.is-center {
            stroke: rgba(255,255,255,0.9);
            stroke-width: 4.2;
        }

    .tv-arena-pressure-gauge__tick-line--minor {
        stroke-width: 2.1;
        opacity: .62;
    }

    .tv-arena-pressure-gauge__tick-badge {
        position: absolute;
        transform: translate(-50%, -50%);
        color: rgba(235, 245, 255, .92);
        font-size: clamp(.82rem, 1.25cqi, 1.14rem);
        font-weight: 950;
        letter-spacing: .055em;
        white-space: nowrap;
        text-shadow: 0 0 10px rgba(0, 0, 0, .65), 0 0 14px rgba(255, 255, 255, .14);
    }

        .tv-arena-pressure-gauge__tick-badge.is-center {
            color: #ffffff;
            font-size: clamp(.86rem, 1.35cqi, 1.2rem);
        }

    .tv-arena-pressure-gauge__split-marker {
        stroke: rgba(247, 251, 255, 0.92);
        stroke-width: 5;
        stroke-linecap: round;
        filter: drop-shadow(0 0 11px color-mix(in srgb, var(--pressure-left) 20%, var(--pressure-right) 20%));
    }

    .tv-arena-pressure-gauge__duel,
    .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__duel {
        position: absolute;
        inset: 53% 0 auto;
        z-index: 3;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: clamp(0.45rem, 2.1cqi, 1.05rem);
        pointer-events: none;
    }

    .tv-arena-pressure-gauge__ball {
        width: clamp(52px, 5.6cqi, 78px);
        aspect-ratio: 1;
        overflow: hidden;
        border: 1px solid rgba(255,255,255,0.22);
        border-radius: 50%;
        box-shadow: 0 0 18px rgba(255,255,255,0.18), 0 0 32px rgba(46, 232, 242, 0.18), inset 0 0 0 2px rgba(255,255,255,0.18);
        animation: tvArenaPressureBallSpin var(--ball-spin-duration, 3s) linear infinite;
    }

    .tv-arena-pressure-gauge__ball--small {
        width: 16px;
        box-shadow: 0 0 10px rgba(255,255,255,0.18), 0 0 16px rgba(46, 232, 242, 0.14), inset 0 0 0 1px rgba(255,255,255,0.15);
    }

    .tv-arena-pressure-gauge__ball-image {
        display: block;
        width: 100%;
        height: auto;
        object-fit: contain;
        user-select: none;
        -webkit-user-drag: none;
        filter: drop-shadow(0 0 10px rgba(255,255,255,0.26));
    }

    @@keyframes tvArenaPressureBallSpin {
        from {
            transform: translateX(var(--ball-lean, 0px)) rotate(0deg);
        }

        to {
            transform: translateX(var(--ball-lean, 0px)) rotate(calc(360deg * var(--ball-spin-direction, 1)));
        }
    }

    .tv-arena-pressure-gauge__arrow {
        position: relative;
        display: inline-block;
        width: clamp(44px, 6.5cqi, 86px);
        height: 14px;
        opacity: 0.9;
    }

        .tv-arena-pressure-gauge__arrow::before,
        .tv-arena-pressure-gauge__arrow::after {
            content: "";
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
        }

        .tv-arena-pressure-gauge__arrow::before {
            width: calc(100% - 14px);
            height: 5px;
            border-radius: 999px;
        }

        .tv-arena-pressure-gauge__arrow::after {
            width: 0;
            height: 0;
            border-top: 10px solid transparent;
            border-bottom: 10px solid transparent;
        }

    .tv-arena-pressure-gauge__arrow--left::before {
        right: 0;
        background: linear-gradient(90deg, rgba(255, 138, 31, 0), rgba(255, 138, 31, 1));
        box-shadow: 0 0 18px color-mix(in srgb, var(--pressure-left) 32%, transparent);
    }

    .tv-arena-pressure-gauge__arrow--left::after {
        left: 0;
        border-right: 16px solid var(--pressure-left);
    }

    .tv-arena-pressure-gauge__arrow--right::before {
        left: 0;
        background: linear-gradient(90deg, rgba(46, 232, 242, 1), rgba(46, 232, 242, 0));
        box-shadow: 0 0 18px color-mix(in srgb, var(--pressure-right) 32%, transparent);
    }

    .tv-arena-pressure-gauge__arrow--right::after {
        right: 0;
        border-left: 16px solid var(--pressure-right);
    }

    .tv-arena-pressure-gauge__center-copy,
    .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__center-copy {
        position: absolute;
        left: 50%;
        top: 66%;
        z-index: 4;
        transform: translateX(-50%);
        display: grid;
        justify-items: center;
        gap: 0.18rem;
        min-width: min(90%, 250px);
        text-align: center;
    }

        .tv-arena-pressure-gauge__center-copy span,
        .tv-arena-pressure-gauge__center-copy em {
            color: rgba(226, 239, 255, .88);
            font-size: clamp(.86rem, 1.22cqi, 1.1rem);
            font-style: normal;
            font-weight: 950;
            letter-spacing: .105em;
            text-shadow: 0 0 10px rgba(0,0,0,.72);
        }

        .tv-arena-pressure-gauge__center-copy strong,
        .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__center-copy strong {
            color: var(--pressure-right);
            font-size: clamp(2.7rem, 5.9cqi, 4.85rem);
            line-height: 1;
            font-weight: 900;
            letter-spacing: -0.05em;
            text-shadow: 0 0 22px color-mix(in srgb, var(--pressure-right) 32%, transparent);
        }

    .tv-arena-pressure-gauge.left-dominant .tv-arena-pressure-gauge__center-copy strong {
        color: var(--pressure-left);
        text-shadow: 0 0 22px color-mix(in srgb, var(--pressure-left) 30%, transparent);
    }

    .tv-arena-pressure-gauge.right-dominant .tv-arena-pressure-gauge__center-copy strong,
    .tv-arena-pressure-gauge.balanced .tv-arena-pressure-gauge__center-copy strong {
        color: var(--pressure-right);
    }

    .tv-arena-pressure-gauge__footer {
        display: grid;
        grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
        gap: clamp(0.45rem, 0.85cqi, 0.75rem);
        align-items: end;
        min-height: 0;
        padding-top: clamp(0.35rem, 0.65cqi, 0.55rem);
        border-top: 1px solid rgba(255,255,255,0.06);
        margin-top: 0;
    }

    .tv-arena-pressure-gauge__momentum,
    .tv-arena-pressure-gauge__footer-status {
        display: grid;
        gap: 0.32rem;
        min-width: 0;
    }

        .tv-arena-pressure-gauge__momentum span,
        .tv-arena-pressure-gauge__footer-status small {
            color: rgba(212, 228, 245, 0.68);
            font-size: clamp(0.62rem, 1.35cqi, 0.8rem);
            font-weight: 800;
            letter-spacing: 0.12em;
        }

        .tv-arena-pressure-gauge__momentum strong,
        .tv-arena-pressure-gauge__footer-status strong {
            color: #f5fbff;
            font-size: clamp(0.92rem, 1.75cqi, 1.2rem);
            font-weight: 900;
            letter-spacing: 0.07em;
        }

        .tv-arena-pressure-gauge__momentum small {
            color: rgba(212, 228, 245, 0.64);
            font-size: clamp(0.62rem, 1.25cqi, 0.76rem);
            font-weight: 700;
            letter-spacing: 0.1em;
        }

    .tv-arena-pressure-gauge__momentum--right {
        text-align: right;
    }

    .tv-arena-pressure-gauge__bars {
        display: grid;
        grid-auto-flow: column;
        grid-auto-columns: minmax(3px, 1fr);
        align-items: end;
        gap: clamp(2px, 0.55cqi, 4px);
        height: clamp(28px, 4.8cqi, 40px);
        overflow: hidden;
    }

        .tv-arena-pressure-gauge__bars i {
            display: block;
            width: 100%;
            border-radius: 999px 999px 0 0;
            transition: height 420ms ease;
        }

    .tv-arena-pressure-gauge__momentum--left .tv-arena-pressure-gauge__bars i {
        background: linear-gradient(180deg, rgba(255, 191, 60, 1), rgba(255, 107, 0, 0.76));
        box-shadow: 0 0 10px color-mix(in srgb, var(--pressure-left) 26%, transparent);
    }

    .tv-arena-pressure-gauge__momentum--right .tv-arena-pressure-gauge__bars i {
        background: linear-gradient(180deg, rgba(115, 255, 240, 1), rgba(36, 225, 239, 0.72));
        box-shadow: 0 0 10px color-mix(in srgb, var(--pressure-right) 24%, transparent);
    }

    .tv-arena-pressure-gauge__footer-status {
        align-content: center;
        justify-items: center;
        padding-inline: clamp(0.2rem, 1cqi, 0.8rem);
        text-align: center;
    }

    .tv-arena-pressure-gauge__footer-arrows {
        display: flex;
        align-items: center;
        gap: 0.45rem;
    }

        .tv-arena-pressure-gauge__footer-arrows .tv-arena-pressure-gauge__arrow {
            width: 42px;
            height: 10px;
        }

            .tv-arena-pressure-gauge__footer-arrows .tv-arena-pressure-gauge__arrow::after {
                border-top-width: 9px;
                border-bottom-width: 9px;
            }

        .tv-arena-pressure-gauge__footer-arrows .tv-arena-pressure-gauge__arrow--left::after {
            border-right-width: 14px;
        }

        .tv-arena-pressure-gauge__footer-arrows .tv-arena-pressure-gauge__arrow--right::after {
            border-left-width: 14px;
        }

    .tv-arena-pressure-gauge__description {
        display: grid;
        grid-template-columns: auto minmax(0, 1fr);
        gap: 0.85rem;
        align-items: center;
        padding: clamp(0.55rem, 0.95cqi, 0.75rem);
        border: 1px solid rgba(255,255,255,0.07);
        border-radius: 16px;
        background: rgba(7, 16, 30, 0.56);
        margin-top: 0;
    }

    .tv-arena-pressure-gauge__description-icon {
        display: grid;
        place-items: center;
        width: 36px;
        aspect-ratio: 1;
        border: 1px solid color-mix(in srgb, var(--pressure-right) 30%, rgba(255,255,255,0.07));
        border-radius: 50%;
        color: var(--pressure-right);
        font-size: 1.1rem;
        font-weight: 900;
        box-shadow: 0 0 18px color-mix(in srgb, var(--pressure-right) 18%, transparent);
    }

    .tv-arena-pressure-gauge__description p {
        min-width: 0;
        margin: 0;
        color: rgba(213, 228, 245, 0.76);
        font-size: clamp(0.72rem, 1.45cqi, 0.9rem);
        line-height: 1.5;
        overflow-wrap: anywhere;
    }

    .tv-arena-pressure-gauge.critical-pressure .tv-arena-pressure-gauge__panel {
        box-shadow: inset 0 0 0 1px rgba(255,255,255,0.025), 0 24px 54px rgba(0, 0, 0, 0.28), 0 0 44px color-mix(in srgb, var(--pressure-left) 12%, transparent), 0 0 44px color-mix(in srgb, var(--pressure-right) 12%, transparent);
    }

    .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__panel {
        gap: clamp(0.62rem, 1.15cqi, 0.85rem);
        padding: clamp(0.78rem, 1.35cqi, 1rem);
    }

    @@container arena-pressure (min-width: 900px) {
        .tv-arena-pressure-gauge__gauge-shell,
        .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__gauge-shell {
            width: min(100%, clamp(560px, 58cqi, 760px));
            max-width: 760px;
        }

        .tv-arena-pressure-gauge__track,
        .tv-arena-pressure-gauge__fill,
        .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__track,
        .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__fill {
            stroke-width: 38;
        }

        .tv-arena-pressure-gauge__tick-badge {
            font-size: clamp(.95rem, 1.05cqi, 1.24rem);
        }

            .tv-arena-pressure-gauge__tick-badge.is-center {
                font-size: clamp(1rem, 1.12cqi, 1.3rem);
            }
    }

    @@container arena-pressure (max-width: 760px) {
        .tv-arena-pressure-gauge,
        .tv-arena-pressure-gauge__panel {
            height: auto;
            min-height: 0;
        }

            .tv-arena-pressure-gauge__panel,
            .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__panel {
                grid-template-rows: auto auto auto auto;
                gap: 0.9rem;
                padding: clamp(0.82rem, 3cqi, 1.1rem);
                overflow: hidden;
                border-radius: clamp(20px, 4cqi, 28px);
            }

        .tv-arena-pressure-gauge__scoreboard {
            grid-template-columns: minmax(0, 1fr);
        }

        .tv-arena-pressure-gauge__score-team,
        .tv-arena-pressure-gauge__score-team--right,
        .tv-arena-pressure-gauge__score-center {
            height: auto;
            min-height: 58px;
            max-height: none;
            border-radius: 18px;
        }

        .tv-arena-pressure-gauge__score-team,
        .tv-arena-pressure-gauge__score-team--right {
            grid-template-columns: minmax(0, 1fr) auto;
        }

            .tv-arena-pressure-gauge__score-team--right .tv-arena-pressure-gauge__score-value {
                order: 2;
            }

            .tv-arena-pressure-gauge__score-team--right .tv-arena-pressure-gauge__score-identity {
                order: 1;
                justify-content: flex-start;
            }

            .tv-arena-pressure-gauge__score-team--right .tv-arena-pressure-gauge__score-copy {
                text-align: left;
            }

        .tv-arena-pressure-gauge__score-center {
            order: -1;
        }

        .tv-arena-pressure-gauge__score-value {
            font-size: clamp(1.9rem, 10cqi, 3rem);
        }

        .tv-arena-pressure-gauge__layout,
        .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__layout {
            grid-template-columns: repeat(2, minmax(0, 1fr));
            grid-template-areas:
                "center center"
                "left right";
            gap: clamp(0.7rem, 2.5cqi, 1rem);
            align-items: stretch;
            padding-block: 0.65rem;
        }

        .tv-arena-pressure-gauge__center {
            grid-area: center;
        }

        .tv-arena-pressure-gauge__side--left {
            grid-area: left;
        }

        .tv-arena-pressure-gauge__side--right {
            grid-area: right;
        }

        .tv-arena-pressure-gauge__side {
            align-content: start;
            gap: 0.55rem;
            padding: clamp(0.65rem, 2cqi, 0.9rem);
            border: 1px solid rgba(255, 255, 255, 0.06);
            border-radius: 18px;
            background: rgba(4, 11, 24, 0.34);
        }

        .tv-arena-pressure-gauge__side--left,
        .tv-arena-pressure-gauge__side--right {
            justify-items: stretch;
        }

        .tv-arena-pressure-gauge__side--left {
            text-align: left;
        }

        .tv-arena-pressure-gauge__side--right {
            text-align: right;
        }

            .tv-arena-pressure-gauge__identity,
            .tv-arena-pressure-gauge__side--right .tv-arena-pressure-gauge__identity {
                width: 100%;
                flex-direction: column;
                gap: 0.5rem;
            }

        .tv-arena-pressure-gauge__identity {
            align-items: flex-start;
        }

        .tv-arena-pressure-gauge__side--right .tv-arena-pressure-gauge__identity {
            align-items: flex-end;
        }

        .tv-arena-pressure-gauge__gauge-shell,
        .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__gauge-shell {
            width: min(100%, 420px);
            max-width: 420px;
            aspect-ratio: 420 / 318;
        }

        .tv-arena-pressure-gauge__track,
        .tv-arena-pressure-gauge__fill,
        .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__track,
        .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__fill {
            stroke-width: 28;
        }

        .tv-arena-pressure-gauge__tick-badge {
            font-size: clamp(.72rem, 2.4cqi, .98rem);
        }

        .tv-arena-pressure-gauge__center-copy,
        .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__center-copy {
            top: 68%;
        }

            .tv-arena-pressure-gauge__center-copy span,
            .tv-arena-pressure-gauge__center-copy em {
                font-size: clamp(.7rem, 2.2cqi, .92rem);
            }

        .tv-arena-pressure-gauge__footer {
            grid-template-columns: repeat(2, minmax(0, 1fr));
            grid-template-areas:
                "momentum-left momentum-right"
                "status status";
            gap: 0.9rem;
            align-items: stretch;
        }

        .tv-arena-pressure-gauge__momentum--left {
            grid-area: momentum-left;
        }

        .tv-arena-pressure-gauge__momentum--right {
            grid-area: momentum-right;
            text-align: right;
        }

        .tv-arena-pressure-gauge__footer-status {
            grid-area: status;
            justify-items: center;
            padding-top: 0.75rem;
            border-top: 1px solid rgba(255, 255, 255, 0.06);
            text-align: center;
        }
    }

    @@container arena-pressure (max-width: 430px) {
        .tv-arena-pressure-gauge__panel,
        .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__panel {
            gap: 0.78rem;
            padding: 0.78rem;
            border-radius: 20px;
        }

        .tv-arena-pressure-gauge__header h3,
        .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__header h3 {
            font-size: clamp(0.96rem, 7cqi, 1.35rem);
            letter-spacing: 0.12em;
        }

        .tv-arena-pressure-gauge__header p {
            font-size: 0.62rem;
            letter-spacing: 0.08em;
        }

        .tv-arena-pressure-gauge__layout,
        .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__layout {
            grid-template-columns: 1fr;
            grid-template-areas:
                "center"
                "left"
                "right";
        }

        .tv-arena-pressure-gauge__side,
        .tv-arena-pressure-gauge__side--left,
        .tv-arena-pressure-gauge__side--right {
            justify-items: stretch;
            text-align: left;
        }

            .tv-arena-pressure-gauge__identity,
            .tv-arena-pressure-gauge__side--right .tv-arena-pressure-gauge__identity {
                flex-direction: row;
                align-items: center;
            }

        .tv-arena-pressure-gauge__logo-ring,
        .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__logo-ring {
            width: 58px;
        }

        .tv-arena-pressure-gauge__metric {
            grid-template-columns: auto 1fr;
            align-items: baseline;
            column-gap: 0.55rem;
        }

            .tv-arena-pressure-gauge__metric strong {
                font-size: 2rem;
            }

        .tv-arena-pressure-gauge__gauge-shell,
        .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__gauge-shell {
            aspect-ratio: 420 / 340;
        }

        .tv-arena-pressure-gauge__duel,
        .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__duel {
            inset: 52% 0 auto;
        }

        .tv-arena-pressure-gauge__arrow {
            width: clamp(34px, 13cqi, 52px);
            height: 10px;
        }

            .tv-arena-pressure-gauge__arrow::after {
                border-top-width: 9px;
                border-bottom-width: 9px;
            }

        .tv-arena-pressure-gauge__arrow--left::after {
            border-right-width: 14px;
        }

        .tv-arena-pressure-gauge__arrow--right::after {
            border-left-width: 14px;
        }

        .tv-arena-pressure-gauge__ball {
            width: 44px;
        }

        .tv-arena-pressure-gauge__center-copy,
        .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__center-copy {
            top: 68%;
        }

            .tv-arena-pressure-gauge__center-copy strong,
            .tv-arena-pressure-gauge.is-compact .tv-arena-pressure-gauge__center-copy strong {
                font-size: clamp(1.9rem, 14cqi, 2.9rem);
            }

        .tv-arena-pressure-gauge__footer {
            grid-template-columns: 1fr;
            grid-template-areas:
                "momentum-left"
                "momentum-right"
                "status";
        }

        .tv-arena-pressure-gauge__momentum--right {
            text-align: left;
        }

        .tv-arena-pressure-gauge__description {
            grid-template-columns: 1fr;
            padding: 0.78rem;
        }

        .tv-arena-pressure-gauge__description-icon {
            width: 30px;
        }
    }

    @@media (prefers-reduced-motion: reduce) {
        .tv-arena-pressure-gauge__ball {
            animation: none;
        }

        .tv-arena-pressure-gauge__fill,
        .tv-arena-pressure-gauge__bars i {
            transition: none;
        }
    }
</style>